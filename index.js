/**
 * @param  {string} start
 * @param  {string} end
 * @param  {string[]} bank
 * @return {number}
 */
var minMutation = function(start, end, bank) {
	if (bank == undefined) {
		return -1;
	}

	return 1;
};

module.exports = {
	minMutation: minMutation
};